#include<bits/stdc++.h>
using namespace std;
int main()
{
   int a,b, lcm = 0;
   cout<<"enter two numbers" <<endl;
   cin>>a>>b;
   lcm = (a>b)?a:b;
   if(lcm%a==0 && lcm%b==0)
   {
       cout<<lcm<<endl;
       
   }
   lcm++;
   return 0;
}

enter two numbers
21 7
21
